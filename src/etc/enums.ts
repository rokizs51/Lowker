export enum JobType {
    FULL_TIME = 'full_time',
    PART_TIME = 'part_time',
    CONTRACT = 'contract',
    INTERNSHIP = 'internship'
}

export enum JobGender {
    Male = 'male',
    Female = 'female',
    any = 'any'
}

export enum Role {
    EMPLOYER = 'employer',
    JOB_SEEKER = 'job_seeker',
}

  
